export default async function DashboardPage() {
  // const response = await fetch(`${process.env.NEXT_PUBLIC_API}/get-data`, {
  //   next: { tags: ['data'] }
  // })

  return <div className='grid gap-4'>Dashboard</div>
}
